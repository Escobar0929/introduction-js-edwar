
"use strict"
 const numbers = [0, 5, 8, 9, 5, 8, 71, 120, 500];

// // Agregar elemtentos a mi arreglo, pero con este metodo se requiere conocer la longitud del arrglo
// numbersLenght = numbers.length
// console.log(numbersLenght);

// numbers [numbersLenght] = 100;

// console.log(numbers);

// con el metodo push se agregan elemtnos al final del arrglo 
// numbers.push(700);

// console.log(numbers);

// metodo unshift para agregar elmentos al inicio de un arrglo
numbers.unshift(-3, -2, -1);

// Método pop para eliminar el ultimó elemento de mi arreglo
// numbers.pop();

// Método unshift para eliminar el primer elemento de mi arreglo
// numbers.unshift();

// Método splice elimina todos los elementos apartir del indice asignado 
// numbers.splice(2);
console.table(numbers);


